apply plugin: 'java'

repositories {
	mavenCentral()
	maven {
		url 'https://oss.sonatype.org/content/repositories/snapshots'
	}
}

dependencies {
	compile 'com.madisp.stupid:stupid:0.1.0-SNAPSHOT'
}

task wrapper(type: Wrapper) {
	gradleVersion = '1.10'
}
